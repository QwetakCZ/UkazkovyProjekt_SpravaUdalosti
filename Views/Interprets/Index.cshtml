@model IEnumerable<SpravaUdalosti.Models.Interprets>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NazevInterpreta)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PopisInterpreta)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.HudebniZanr)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ZemePuvodu)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NazevInterpreta)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PopisInterpreta)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.HudebniZanr)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ZemePuvodu)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
